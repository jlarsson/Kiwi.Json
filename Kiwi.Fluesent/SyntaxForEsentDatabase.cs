using System;
using Microsoft.Isam.Esent.Interop;

namespace Kiwi.Fluesent
{
    public static class SyntaxForEsentDatabase
    {
        //public static IEsentInstance CreateInstance(this IEsentDatabase database)
        //{
        //    return database.CreateInstance(Guid.NewGuid().ToString("n"), null, InitGrbit.None);
        //}
        //public static IEsentInstance CreateInstance(this IEsentDatabase database, InitGrbit grbit)
        //{
        //    return database.CreateInstance(Guid.NewGuid().ToString("n"), null, grbit);
        //}
        //public static IEsentInstance CreateInstance(this IEsentDatabase database, string displayName, InitGrbit grbit)
        //{
        //    return database.CreateInstance(Guid.NewGuid().ToString("n"), displayName, grbit);
        //}
    }
}